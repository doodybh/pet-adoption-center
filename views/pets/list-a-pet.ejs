<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>List Your Pet</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>

  <body class="bg-light">
    <%- include("../navbar") %>

    <main class="container py-4">
      <div class="card shadow-sm">
        <div class="card-body p-4">
          <h1 class="h4 mb-3">List Your Pet</h1>

          <% if (typeof error !== "undefined" && error) { %>
            <div class="alert alert-danger"><%= error %></div>
          <% } %>

          <form
            action="/pets/list-a-pet"
            method="POST"
            enctype="multipart/form-data"
            class="needs-validation"
            novalidate
          >
            <div class="mb-3">
              <label class="form-label">Name</label>
              <input
                class="form-control"
                type="text"
                name="name"
                required
                minlength="2"
                maxlength="30"
                data-msg-required="Please enter the pet's name."
                data-msg-min="Name must be at least 2 characters."
                data-msg-max="Name must be 30 characters or less."
              />
              <div class="invalid-feedback"></div>
            </div>

            <div class="mb-3">
              <label class="form-label">Category</label>
              <select
                class="form-select"
                name="category"
                required
                data-msg-required="Please choose a category."
              >
                <option value="">Select…</option>
                <option value="Fluffies">Fluffies</option>
                <option value="Chripers">Chripers</option>
                <option value="Slitherers">Slitherers</option>
                <option value="Pocket Pets">Pocket Pets</option>
                <option value="Fishies">Fishies</option>
                <option value="Others">Others</option>
              </select>
              <div class="invalid-feedback"></div>
            </div>

            <div class="mb-3">
              <label class="form-label">Date of Birth</label>
              <input
                class="form-control"
                type="date"
                name="dob"
                id="dob"
                min="<%= new Date(new Date().setFullYear(new Date().getFullYear() - 50)).toISOString().slice(0, 10) %>"
                max="<%= new Date().toISOString().slice(0, 10) %>"
                required
                data-msg-required="Please choose the pet’s date of birth."
              />
            <div class="invalid-feedback"></div>

            <div class="invalid-feedback"></div>

              <div class="invalid-feedback"></div>
            </div>

            <div class="mb-3">
              <label class="form-label">Age</label>
              <div class="form-control bg-body-secondary" data-age-from-dob="#dob">—</div>
            </div>

            <div class="mb-3">
              <label class="form-label">Description</label>
              <input
                class="form-control"
                type="text"
                name="description"
                required
                minlength="10"
                maxlength="200"
                data-msg-required="Please write a short description."
                data-msg-min="Description must be at least 10 characters."
                data-msg-max="Description must be 200 characters or less."
              />
              <div class="invalid-feedback"></div>
            </div>

            <div class="mb-3">
              <label class="form-label">Picture</label>
              <input
                class="form-control"
                type="file"
                name="picture"
                accept="image/*"
                required
                data-msg-required="Please upload a picture of the pet."
              />
              <div class="invalid-feedback"></div>
            </div>

            <button class="btn btn-primary" type="submit">List</button>
          </form>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/forms.js"></script>
  </body>
</html>
