<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Pet</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <style>
      .pet-hero-img {
        width: 100%;
        max-height: 280px;
        object-fit: cover;
        border-radius: 12px;
      }
    </style>
  </head>

  <body class="bg-light">
    <%- include("../navbar") %>

    <main class="container py-4">
      <div class="card shadow-sm">
        <div class="card-body p-4">
          <h1 class="h4 mb-3">Edit Pet</h1>

          <% if (pet.picture) { %>
            <img
              src="<%= pet.picture %>"
              alt="<%= pet.name %>"
              class="pet-hero-img mb-3"
            />
          <% } %>

          <form
            action="/pets/<%= pet._id %>/edit"
            method="POST"
            enctype="multipart/form-data"
            class="needs-validation"
            novalidate
          >
            <div class="mb-3">
              <label class="form-label">Name</label>
              <input
                class="form-control"
                name="name"
                value="<%= pet.name %>"
                required
                minlength="2"
                maxlength="30"
                data-msg-required="Please enter the pet's name."
                data-msg-min="Name must be at least 2 characters."
                data-msg-max="Name must be 30 characters or less."
              />
              <div class="invalid-feedback"></div>
            </div>

            <div class="mb-3">
              <label class="form-label">Category</label>
              <select
                class="form-select"
                name="category"
                required
                data-msg-required="Please choose a category."
              >
                <option value="Fluffies" <%= pet.category == "Fluffies" ? "selected" : "" %>>Fluffies</option>
                <option value="Chripers" <%= pet.category == "Chripers" ? "selected" : "" %>>Chripers</option>
                <option value="Slitherers" <%= pet.category == "Slitherers" ? "selected" : "" %>>Slitherers</option>
                <option value="Pocket Pets" <%= pet.category == "Pocket Pets" ? "selected" : "" %>>Pocket Pets</option>
                <option value="Fishies" <%= pet.category == "Fishies" ? "selected" : "" %>>Fishies</option>
                <option value="Others" <%= pet.category == "Others" ? "selected" : "" %>>Others</option>
              </select>
              <div class="invalid-feedback"></div>
            </div>

            <div class="mb-3">
              <label class="form-label">Description</label>
              <input
                class="form-control"
                type="text"
                name="description"
                value="<%= pet.description %>"
                required
                minlength="10"
                maxlength="200"
                data-msg-required="Please write a short description."
                data-msg-min="Description must be at least 10 characters."
                data-msg-max="Description must be 200 characters or less."
              />
              <div class="invalid-feedback"></div>
            </div>

            <div class="mb-3">
              <label class="form-label">Date of Birth</label>
              <input
                class="form-control"
                type="date"
                name="dob"
                id="dob"
                value="<%= pet.dob.toISOString().slice(0, 10) %>"
                min="<%= new Date(new Date().setFullYear(new Date().getFullYear() - 50)).toISOString().slice(0, 10) %>"
                max="<%= new Date().toISOString().slice(0, 10) %>"
                required
                data-msg-required="Please choose the pet’s date of birth."
              />
            <div class="invalid-feedback"></div>

              <div class="invalid-feedback"></div>
            </div>

            <div class="mb-3">
              <label class="form-label">Age</label>
              <div
                class="form-control bg-body-secondary"
                data-age-from-dob="#dob"
              >
                —
              </div>
            </div>

            <div class="mb-3">
              <label class="form-label">Change picture</label>
              <input
                class="form-control"
                type="file"
                name="picture"
                accept="image/*"
              />
            </div>

            <button class="btn btn-primary" type="submit">Save</button>
            <a class="btn btn-outline-secondary ms-2" href="/pets/my-pets">Cancel</a>
          </form>

          <hr class="my-4" />

          <button
            class="btn btn-danger"
            type="button"
            data-bs-toggle="modal"
            data-bs-target="#deletePetModal"
          >
            Remove listing for <b><%= pet.name %></b>
          </button>

          <div
            class="modal fade"
            id="deletePetModal"
            tabindex="-1"
            aria-hidden="true"
          >
            <div class="modal-dialog modal-dialog-centered">
              <div class="modal-content">
                <div class="modal-header">
                  <h5 class="modal-title">Remove listing?</h5>
                  <button
                    type="button"
                    class="btn-close"
                    data-bs-dismiss="modal"
                  ></button>
                </div>

                <div class="modal-body">
                  This will remove <b><%= pet.name %></b> from the listings.
                </div>

                <div class="modal-footer">
                  <button
                    type="button"
                    class="btn btn-outline-secondary"
                    data-bs-dismiss="modal"
                  >
                    Cancel
                  </button>

                  <form
                    action="/pets/<%= pet._id %>/delete"
                    method="POST"
                    class="m-0"
                  >
                    <button type="submit" class="btn btn-danger">
                      Remove
                    </button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/forms.js"></script>
  </body>
</html>
