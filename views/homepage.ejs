<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>

  <body class="bg-light">
    <%- include("./navbar") %>

    <main class="container py-5">
      <div class="row justify-content-center">
        <div class="col-12 col-lg-8">
          <div class="card shadow-sm">
            <div class="card-body p-4">
              <h1 class="h3 mb-2">PetTopia</h1>
              <p class="text-muted mb-4">
                Browse pets, view details, and manage your own listings.
              </p>

              <div class="d-flex gap-2 flex-wrap">
                <a class="btn btn-primary" href="/pets">Browse Pets</a>

                <% if (user) { %>
                  <a class="btn btn-outline-secondary" href="/pets/my-pets">My Pets</a>
                  <a class="btn btn-outline-primary" href="/pets/list-a-pet">List a pet</a>
                <% } else { %>
                  <a class="btn btn-outline-primary" href="/auth/sign-up">Create an account</a>
                  <a class="btn btn-outline-secondary" href="/auth/sign-in">Login</a>
                <% } %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
